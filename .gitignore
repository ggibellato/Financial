#folders in any place
.vs
bin
obj
.vite
node_modules
dist
docs/app-*.md

#data directory - real data files are git-ignored, example files are tracked
/data/*
!/data/*.example.json

#infrastructure
.terraform
terraform*
.terraform*

#local deployment output (manual, not part of CI/CD)
/deploy
scripts/deploy.local.json


#test + coverage output (CI regenerates these every run)
TestResults
CoverageReport
.tools

#specific files
.gitignore.bak
data-investment.json
.env*.local
Financial.Web/.env
