<UserControl x:Class="Financial.Presentation.App.Views.Investment.AssetPriceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Read Assets Current Values" FontSize="20" FontWeight="Bold"
                   Foreground="#333333" Margin="0,0,0,20"/>

        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15"
                Margin="0,0,0,20" Background="#FAFAFA">
            <!-- Scoped to this Border, not the whole UserControl, so it only affects the
                 Check Prices button below - the results DataGrid in Grid.Row="2" keeps its
                 existing (non-Fluent) styling untouched by this fix. -->
            <Border.Resources>
                <ResourceDictionary>
                    <!-- Matches Financial.Web's accent — see ExpenseFormView.xaml / ADR-005. -->
                    <SolidColorBrush x:Key="AccentButtonBackground" Color="#0F6CBD"/>
                    <SolidColorBrush x:Key="AccentButtonBackgroundPointerOver" Color="#115EA3"/>
                    <SolidColorBrush x:Key="AccentButtonBackgroundPressed" Color="#0C3B5E"/>
                </ResourceDictionary>
            </Border.Resources>
            <StackPanel>
                <TextBlock Text="Fetch Current Prices" FontSize="14" FontWeight="Bold"
                           Foreground="#333333" Margin="0,0,0,8"/>
                <ui:Button Content="Check Prices" Appearance="Primary" Icon="{ui:SymbolIcon Symbol=Search20}"
                           Command="{Binding FetchCommand}" HorizontalAlignment="Left"/>

                <StackPanel Visibility="{Binding IsFetching, Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="0,15,0,0">
                    <ProgressBar Height="8" Minimum="0" Maximum="100"
                                 Value="{Binding ProgressPercent, Mode=OneWay}" Foreground="#007ACC"/>
                    <TextBlock Text="{Binding ProgressMessage}" FontSize="13"
                               Foreground="#666666" Margin="0,8,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Assets Current Prices" FontSize="16" FontWeight="Bold"
                           Foreground="#333333" Margin="0,0,0,10"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Results}"
                          BorderBrush="#CCCCCC" BorderThickness="1"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Ticker}" Header="Ticker" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource PlainColumnTextStyle}">
                                    <Setter Property="Padding" Value="8"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource PlainColumnTextStyle}">
                                    <Setter Property="Padding" Value="8"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Price, StringFormat=N2}" Header="Price" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource NumericColumnTextStyle}">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Error}" Header="Status" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasError}" Value="True">
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
