<UserControl x:Class="Financial.Presentation.App.Views.Investment.DividendCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Shares Dividend Check" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,20"/>

        <Border Grid.Row="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" Padding="15"
                Margin="0,0,0,20" Background="{DynamicResource ControlFillColorDefaultBrush}">
            <StackPanel>
                <TextBlock Text="Select Ticker" FontSize="14" FontWeight="Bold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,8"/>
                <Grid>
                    <!-- Scoped to this Grid, not the whole UserControl, so it only affects the
                         Check button below - the view's two DataGrids elsewhere keep their
                         existing (non-Fluent) styling untouched by this fix. -->
                    <Grid.Resources>
                        <ResourceDictionary>
                            <!-- Matches Financial.Web's accent — see ExpenseFormView.xaml / ADR-005. -->
                            <SolidColorBrush x:Key="AccentButtonBackground" Color="#0F6CBD"/>
                            <SolidColorBrush x:Key="AccentButtonBackgroundPointerOver" Color="#115EA3"/>
                            <SolidColorBrush x:Key="AccentButtonBackgroundPressed" Color="#0C3B5E"/>
                        </ResourceDictionary>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="txtTicker" Grid.Column="0" DisplayMemberPath="Name"
                              IsEditable="True" SelectedIndex="0" Height="32"
                              Text="{Binding Ticker, UpdateSourceTrigger=PropertyChanged}"
                              VerticalContentAlignment="Center" Padding="8,0">
                        <ComboBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="0,0,0,1" Padding="5">
                                            <TextBlock FontWeight="Bold" Text="{Binding Name}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        </Border>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ComboBox.GroupStyle>
                    </ComboBox>
                    <ui:Button Grid.Column="2" Content="Check" Appearance="Primary"
                               Icon="{ui:SymbolIcon Symbol=Search20}"
                               Command="{Binding CheckCommand}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Matches the app-wide error display convention (see MonthlyView.xaml) rather than the
             bordered/tinted banner this used to be - no other view in the app has a background-
             filled error banner, and Wpf.Ui has no theme-aware "critical background" brush to
             build one from. -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Margin="0,0,0,20"
                   Foreground="{DynamicResource SystemFillColorCriticalBrush}" FontWeight="Bold" TextWrapping="Wrap"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <Border Grid.Row="3" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding SummaryName, FallbackValue='Select a ticker and click Check'}"
                           FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,10"/>
                <TextBlock Grid.Row="1" Text="{Binding SummaryPrice, FallbackValue='Current price: --'}"
                           FontSize="14" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="2" Text="{Binding SummaryAverageDividend, FallbackValue='Average Dividend: --'}"
                           FontSize="14" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="3" Text="{Binding SummaryDividendYield, FallbackValue='Dividend Yield: --'}"
                           FontSize="14" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="4" Text="{Binding SummaryPriceMaxBuy, FallbackValue='Price max buy: --'}"
                           FontSize="14" FontWeight="Bold">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPriceGood}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="300"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*" MinWidth="200"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Dividend History" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,10"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding History}"
                              AutoGenerateColumns="False"
                              BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Type}" Header="Type"
                                                 ElementStyle="{StaticResource PlainColumnTextStyle}"/>
                            <DataGridTextColumn Binding="{Binding Date, Converter={StaticResource DateFormatConverter}}" Header="Date"
                                                 ElementStyle="{StaticResource PlainColumnTextStyle}"/>
                            <DataGridTextColumn Binding="{Binding Value, StringFormat=N2}" Header="Value">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource NumericColumnTextStyle}">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="By Year" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,10"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding YearTotals}"
                              AutoGenerateColumns="False"
                              BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Year}" Header="Year"
                                                 ElementStyle="{StaticResource PlainColumnTextStyle}"/>
                            <DataGridTextColumn Binding="{Binding Total, StringFormat=N2}" Header="Total">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource NumericColumnTextStyle}">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
