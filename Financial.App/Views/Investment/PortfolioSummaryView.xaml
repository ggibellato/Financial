<UserControl x:Class="Financial.Presentation.App.Views.Investment.PortfolioSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Financial.Presentation.App.Views.Investment">
    <UserControl.Resources>
        <DataTemplate x:Key="AssetSummaryTemplate">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Quantity:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AssetDetails.Quantity, Mode=OneWay, StringFormat=N8}" TextAlignment="Right" FontFamily="Consolas" Margin="10,5"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Average Price:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding AssetDetails.AveragePrice, Mode=OneWay, StringFormat=N2}" TextAlignment="Right" FontFamily="Consolas" Margin="10,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="ISIN:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AssetDetails.ISIN, Mode=OneWay}" Margin="10,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Country:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding AssetDetails.Country, Mode=OneWay}" Margin="10,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Local Type:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AssetDetails.LocalTypeCode, Mode=OneWay}" Margin="10,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Asset Class:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding AssetDetails.Class, Mode=OneWay}" Margin="10,5"/>

                    <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4"
                            BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Margin="0,15,0,15"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Total Bought:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="4" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalBought, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>

                    <TextBlock Grid.Row="4" Grid.Column="2" Text="Total Sold:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="4" Grid.Column="3"
                               Text="{Binding AssetDetails.TotalSold, Mode=OneWay, StringFormat=N2}"
                               Margin="10,5"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Total Credits:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="5" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalCredits, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Foreground="Blue"/>

                    <Border Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="4"
                            BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Margin="0,15,0,15"/>

                    <TextBlock Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Text="Realized" FontWeight="Bold" FontSize="13" Margin="0,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Realized Gain/Loss:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="8" Grid.Column="1"
                               Text="{Binding AssetDetails.RealizedGainLoss, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.RealizedGainLoss, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="8" Grid.Column="2" Text="Portfolio Weight:" FontWeight="Bold" Margin="20,5,0,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="8" Grid.Column="3"
                               Text="{Binding AssetDetails.DisplayRealizedPortfolioWeight, Mode=OneWay}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="9" Grid.Column="0" Text="XIRR:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="9" Grid.Column="1"
                               Text="{Binding AssetDetails.RealizedXirr, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.RealizedXirr, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="9" Grid.Column="2" Text="XIRR w/ Credits:" FontWeight="Bold" Margin="20,5,0,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="9" Grid.Column="3"
                               Text="{Binding AssetDetails.RealizedXirrWithCredits, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.RealizedXirrWithCredits, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.IsHistoricScope, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="2" Text="Current" FontWeight="Bold" FontSize="13" Margin="0,5"
                               Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <!-- Scoped to this Border, not the whole UserControl, so it only affects the
                         Refresh button - the AssetSummary DataGrid elsewhere in this view keeps
                         its existing (non-Fluent) styling untouched by this fix. -->
                    <Border Grid.Row="10" Grid.Column="2" Grid.ColumnSpan="2"
                            HorizontalAlignment="Left"
                            Margin="10,5,0,5"
                            Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border.Resources>
                            <ResourceDictionary>
                                <!-- Matches Financial.Web's accent — see ExpenseFormView.xaml / ADR-005. -->
                                <SolidColorBrush x:Key="AccentButtonBackground" Color="#0F6CBD"/>
                                <SolidColorBrush x:Key="AccentButtonBackgroundPointerOver" Color="#115EA3"/>
                                <SolidColorBrush x:Key="AccentButtonBackgroundPressed" Color="#0C3B5E"/>
                            </ResourceDictionary>
                        </Border.Resources>
                        <ui:Button Content="Refresh" Appearance="Primary" Icon="{ui:SymbolIcon Symbol=ArrowSync20}"
                                   Command="{Binding AssetDetails.RefreshTodayInfoCommand}"/>
                    </Border>

                    <TextBlock Grid.Row="11" Grid.Column="0" Text="Current Value:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <StackPanel Grid.Row="11" Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="10,5"
                                Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="{Binding AssetDetails.TodayCurrentValue, Mode=OneWay, StringFormat=N2}"
                                   TextAlignment="Right"
                                   FontFamily="Consolas"/>
                        <TextBlock Text=" (Manual)"
                                   Foreground="#E65100"
                                   FontStyle="Italic"
                                   ToolTip="This value came from a manually-entered price, not a live fetch."
                                   Visibility="{Binding AssetDetails.TodayCurrentValueIsManual, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                    <TextBlock Grid.Row="11" Grid.Column="2" Text="As of:" FontWeight="Bold" Margin="20,5,0,5"
                               Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <StackPanel Grid.Row="11" Grid.Column="3"
                                Orientation="Horizontal"
                                Margin="10,5"
                                Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="{Binding AssetDetails.TodayCurrentValueAsOf, Mode=OneWay}"/>
                        <TextBlock Text=" (Stale)"
                                   Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                   FontStyle="Italic"
                                   ToolTip="This price is older than the most recent weekday."
                                   Visibility="{Binding AssetDetails.IsPriceStale, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <TextBlock Text=" (Unavailable)"
                                   Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                   FontStyle="Italic"
                                   ToolTip="No price or value has been recorded for this holding yet."
                                   Visibility="{Binding AssetDetails.IsPriceUnavailable, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="12" Grid.Column="0" Text="Total Current Value:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="12" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalCurrentValue, Mode=OneWay, StringFormat=N2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="12" Grid.Column="2" Text="Result %:" FontWeight="Bold" Margin="20,5,0,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="12" Grid.Column="3"
                               Text="{Binding AssetDetails.ResultPercent, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.ResultPercent, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="13" Grid.Column="0" Text="Total Current + Credits:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="13" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalCurrentValueWithCredits, Mode=OneWay, StringFormat=N2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="13" Grid.Column="2" Text="Result %:" FontWeight="Bold" Margin="20,5,0,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="13" Grid.Column="3"
                               Text="{Binding AssetDetails.ResultPercentWithCredits, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.ResultPercentWithCredits, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="14" Grid.Column="0" Text="XIRR:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="14" Grid.Column="1"
                               Text="{Binding AssetDetails.Xirr, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.Xirr, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="14" Grid.Column="2" Text="XIRR w/ Credits:" FontWeight="Bold" Margin="20,5,0,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="14" Grid.Column="3"
                               Text="{Binding AssetDetails.XirrWithCredits, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.XirrWithCredits, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"
                               Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="15" Grid.Column="0" Text="Status:" FontWeight="Bold" Margin="0,5"
                               Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Grid.Row="15" Grid.Column="1" Grid.ColumnSpan="3"
                               Text="{Binding AssetDetails.TodayInfoMessage, Mode=OneWay}"
                               Visibility="{Binding AssetDetails.IsActiveScope, Converter={StaticResource BoolToVisibilityConverter}}"
                               Margin="10,5"
                               Foreground="DarkRed"/>

                    <TextBlock Grid.Row="16" Grid.Column="0" Text="Tax Jurisdiction:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="16" Grid.Column="1" Text="{Binding AssetDetails.TaxJurisdictionsDisplay, Mode=OneWay}" Margin="10,5"/>
                </Grid>
            </ScrollViewer>
        </DataTemplate>
        <DataTemplate x:Key="BrokerSummaryTemplate">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Bought:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalBought, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Total Sold:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="3"
                               Text="{Binding AssetDetails.TotalSold, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Total Credits:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalCredits, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"
                               Foreground="Blue"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Total Invested:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="3"
                               Text="{Binding AssetDetails.TotalInvested, Mode=OneWay, StringFormat=N2}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.TotalInvested, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Market Value:" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1"
                               Text="{Binding AssetDetails.MarketValue, Mode=OneWay, StringFormat=N2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Margin="10,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Price-Only Return:" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="3"
                               Text="{Binding AssetDetails.PriceOnlyReturn, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.PriceOnlyReturn, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Total Return (Gross):" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Grid.Row="3" Grid.Column="1"
                               Text="{Binding AssetDetails.TotalReturn, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.TotalReturn, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="2" Text="Total Return (Net of Tax):" FontWeight="Bold" Margin="20,5,0,5"/>
                    <TextBlock Grid.Row="3" Grid.Column="3"
                               Text="{Binding AssetDetails.TotalReturnNetOfTax, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                               TextAlignment="Right"
                               FontFamily="Consolas"
                               Foreground="{Binding AssetDetails.TotalReturnNetOfTax, Converter={StaticResource SignedValueToBrushConverter}}"
                               Margin="10,5"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4"
                               Text="{Binding AssetDetails.IncompleteValuationMessage, Mode=OneWay}"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                               FontStyle="Italic"
                               TextWrapping="Wrap"
                               Margin="0,5"
                               AutomationProperties.LiveSetting="Polite"
                               Visibility="{Binding AssetDetails.HasIncompleteValuation, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4"
                               Text="Loading breakdown..."
                               Margin="0,15,0,5"
                               Visibility="{Binding AssetDetails.IsBreakdownLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="4"
                               Text="{Binding AssetDetails.BreakdownError}"
                               Foreground="DarkRed"
                               Margin="0,15,0,5"
                               Visibility="{Binding AssetDetails.HasBreakdownError, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="4"
                               Text="No active portfolios to display"
                               Foreground="Gray"
                               Margin="0,15,0,5"
                               Visibility="{Binding AssetDetails.ShowBreakdownEmptyState, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="4"
                                Margin="0,15,0,0"
                                Visibility="{Binding AssetDetails.HasBreakdownData, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Portfolio Breakdown" FontWeight="Bold" Margin="0,0,0,5"/>
                        <oxy:PlotView Model="{Binding AssetDetails.OverallBreakdownPlotModel}" Height="260" Background="Transparent"/>
                    </StackPanel>

                    <ItemsControl Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="4"
                                  Margin="0,15,0,0"
                                  ItemsSource="{Binding AssetDetails.PortfolioBreakdownPieItems}"
                                  Visibility="{Binding AssetDetails.HasBreakdownData, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,15,15">
                                    <TextBlock Text="{Binding PortfolioName}" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <oxy:PlotView Model="{Binding PlotModel}" Height="260" Background="Transparent"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <StackPanel Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,15,0,0"
                                Visibility="{Binding AssetDetails.IsReportingCurrencyAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding AssetDetails.ReportingCurrency, StringFormat='Converted to {0}'}" FontWeight="Bold" FontSize="13" Margin="0,0,0,8"/>
                        <TextBlock Text="Some figures could not be converted; showing a partial total."
                                   Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                   FontStyle="Italic"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8"
                                   AutomationProperties.LiveSetting="Polite"
                                   Visibility="{Binding AssetDetails.IsPartial, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Invested:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding AssetDetails.ConvertedInvested, Mode=OneWay, StringFormat=N2, TargetNullValue='—'}"
                                       TextAlignment="Right" FontFamily="Consolas" Margin="10,5"
                                       Foreground="{Binding AssetDetails.ConvertedInvested, Converter={StaticResource SignedValueToBrushConverter}}"/>

                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Market Value:" FontWeight="Bold" Margin="20,5,0,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="3"
                                       Text="{Binding AssetDetails.ConvertedMarketValue, Mode=OneWay, StringFormat=N2, TargetNullValue='—'}"
                                       TextAlignment="Right" FontFamily="Consolas" Margin="10,5"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Unrealised Gain/Loss:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="1"
                                       Text="{Binding AssetDetails.ConvertedUnrealisedGainLoss, Mode=OneWay, StringFormat=N2, TargetNullValue='—'}"
                                       TextAlignment="Right" FontFamily="Consolas" Margin="10,5"
                                       Foreground="{Binding AssetDetails.ConvertedUnrealisedGainLoss, Converter={StaticResource SignedValueToBrushConverter}}"/>

                            <TextBlock Grid.Row="1" Grid.Column="2" Text="Total Return:" FontWeight="Bold" Margin="20,5,0,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="3"
                                       Text="{Binding AssetDetails.ConvertedTotalReturn, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                                       TextAlignment="Right" FontFamily="Consolas" Margin="10,5"
                                       Foreground="{Binding AssetDetails.ConvertedTotalReturn, Converter={StaticResource SignedValueToBrushConverter}}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Total Return (Net of Tax):" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="2" Grid.Column="1"
                                       Text="{Binding AssetDetails.ConvertedTotalReturnNetOfTax, Mode=OneWay, StringFormat=P2, TargetNullValue='—'}"
                                       TextAlignment="Right" FontFamily="Consolas" Margin="10,5"
                                       Foreground="{Binding AssetDetails.ConvertedTotalReturnNetOfTax, Converter={StaticResource SignedValueToBrushConverter}}"/>
                        </Grid>
                    </StackPanel>

                    <TextBlock Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,15,0,0"
                               Text="Converted totals unavailable — showing native-currency figures only."
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                               FontStyle="Italic"
                               TextWrapping="Wrap"
                               AutomationProperties.LiveSetting="Polite"
                               Visibility="{Binding AssetDetails.IsReportingCurrencyUnavailable, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </ScrollViewer>
        </DataTemplate>
        <DataTemplate x:Key="PortfolioSummaryTemplate">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <local:PortfolioTotalsBar Grid.Row="0"/>

                <TextBlock Grid.Row="1"
                           Text="{Binding AssetDetails.IncompleteShareBasisMessage, Mode=OneWay}"
                           Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"
                           AutomationProperties.LiveSetting="Polite"
                           Visibility="{Binding AssetDetails.HasIncompleteShareBasis, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="PortfolioSummaryTemplateHistoric">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <local:PortfolioTotalsBar Grid.Row="0"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl Content="{Binding}">
        <ContentControl.Style>
            <Style TargetType="ContentControl">
                <Setter Property="ContentTemplate" Value="{StaticResource AssetSummaryTemplate}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding AssetDetails.IsPortfolioView}" Value="True">
                        <Setter Property="ContentTemplate" Value="{StaticResource PortfolioSummaryTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding AssetDetails.IsBrokerView}" Value="True">
                        <Setter Property="ContentTemplate" Value="{StaticResource BrokerSummaryTemplate}"/>
                    </DataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding AssetDetails.IsPortfolioView}" Value="True"/>
                            <Condition Binding="{Binding AssetDetails.IsActiveScope}" Value="False"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="ContentTemplate" Value="{StaticResource PortfolioSummaryTemplateHistoric}"/>
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>
        </ContentControl.Style>
    </ContentControl>
</UserControl>
