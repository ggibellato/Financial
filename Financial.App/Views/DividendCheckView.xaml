<UserControl x:Class="Financial.Presentation.App.Views.DividendCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Shares Dividend Check" FontSize="20" FontWeight="Bold"
                   Foreground="#333333" Margin="0,0,0,20"/>

        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15"
                Margin="0,0,0,20" Background="#FAFAFA">
            <StackPanel>
                <TextBlock Text="Select Ticker" FontSize="14" FontWeight="Bold"
                           Foreground="#333333" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="txtTicker" Grid.Column="0" DisplayMemberPath="Name"
                              IsEditable="True" SelectedIndex="0" Height="32"
                              Text="{Binding Ticker, UpdateSourceTrigger=PropertyChanged}"
                              VerticalContentAlignment="Center" Padding="8,0">
                        <ComboBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="5">
                                            <TextBlock FontWeight="Bold" Text="{Binding Name}" Foreground="#666666"/>
                                        </Border>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ComboBox.GroupStyle>
                    </ComboBox>
                    <Button Grid.Column="2" Content="Check" Command="{Binding CheckCommand}"
                            Width="100" Height="32" Foreground="White" FontWeight="Bold"
                            BorderThickness="0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="3" Padding="8,4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#005A9E"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#004578"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding SummaryName, FallbackValue='Select a ticker and click Check'}"
                           FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,10"/>
                <TextBlock Grid.Row="1" Text="{Binding SummaryPrice, FallbackValue='Current price: --'}"
                           FontSize="14" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="2" Text="{Binding SummaryAverageDividend, FallbackValue='Average Dividend: --'}"
                           FontSize="14" Foreground="Blue" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="3" Text="{Binding SummaryDividendYield, FallbackValue='Dividend Yield: --'}"
                           FontSize="14" Foreground="Blue" Margin="0,0,0,5"/>
                <TextBlock Grid.Row="4" Text="{Binding SummaryPriceMaxBuy, FallbackValue='Price max buy: --'}"
                           FontSize="14" FontWeight="Bold">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPriceGood}" Value="True">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="300"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*" MinWidth="200"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Dividend History" FontSize="16" FontWeight="Bold"
                               Foreground="#333333" Margin="0,0,0,10"/>
                    <DataGrid Grid.Row="1" Name="dividendDataGrid" ItemsSource="{Binding History}"
                              AutoGenerateColumns="True" AutoGeneratingColumn="DividendDataGrid_AutoGeneratingColumn"
                              BorderBrush="#CCCCCC" BorderThickness="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="By Year" FontSize="16" FontWeight="Bold"
                               Foreground="#333333" Margin="0,0,0,10"/>
                    <DataGrid Grid.Row="1" Name="dividendByYearDataGrid" ItemsSource="{Binding YearTotals}"
                              AutoGenerateColumns="True" AutoGeneratingColumn="DividendByYearDataGrid_AutoGeneratingColumn"
                              BorderBrush="#CCCCCC" BorderThickness="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
