<UserControl x:Class="Financial.Presentation.App.Components.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             xmlns:datavis="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=DotNetProjects.DataVisualization.Toolkit"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Navigation Tree -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Investments" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Margin="10"
                       Foreground="#333333"/>

            <TreeView Grid.Row="1" 
                      ItemsSource="{Binding RootNodes}"
                      Margin="10,0,10,10"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                        <!-- Keep selection color when control loses focus -->
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                        <Style.Resources>
                            <!-- Override inactive selection color to match active selection -->
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#007ACC"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                        </Style.Resources>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <!-- Icon for Asset nodes based on Active status -->
                            <TextBlock Text="{Binding Metadata[IsActive], Converter={StaticResource BoolToIconConverter}}"
                                       Visibility="{Binding NodeType, Converter={StaticResource NodeTypeToVisibilityConverter}}"
                                       Margin="0,0,5,0"
                                       FontSize="14"
                                       VerticalAlignment="Center"/>

                            <TextBlock Text="{Binding DisplayName}" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <!-- Loading Indicator -->
            <Grid Grid.Row="1" 
                  Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                  Background="#80FFFFFF">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                    <TextBlock Text="Loading..." 
                               HorizontalAlignment="Center" 
                               Margin="0,10,0,0"
                               FontSize="14"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" 
                      Width="5" 
                      HorizontalAlignment="Stretch" 
                      Background="#E0E0E0"/>

        <!-- Right Panel: Asset Details -->
        <Grid Grid.Column="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Asset Header -->
            <Border Grid.Row="0" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="0,0,0,1" 
                    Padding="0,0,0,10"
                    Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding AssetDetails.AssetName, Mode=OneWay}" 
                                       FontSize="20" 
                                       FontWeight="Bold"
                                       Foreground="#333333"/>
                            <Button Command="{Binding AssetDetails.CopyAssetNameCommand}"
                                    ToolTip="Copy asset name"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="2"
                                    Margin="4,0,0,0">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8C8;" FontSize="14" Foreground="#666666"/>
                            </Button>
                        </StackPanel>
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="{Binding AssetDetails.Ticker, Mode=OneWay}"/>
                            <Run>
                                <Run.Text>
                                    <MultiBinding Converter="{StaticResource BetweenTextSeparatorConverter}" ConverterParameter=" · " Mode="OneWay">
                                        <Binding Path="AssetDetails.Ticker" Mode="OneWay"/>
                                        <Binding Path="AssetDetails.Exchange" Mode="OneWay"/>
                                    </MultiBinding>
                                </Run.Text>
                            </Run>
                            <Run Text="{Binding AssetDetails.Exchange, Mode=OneWay}"/>
                            <Run>
                                <Run.Text>
                                    <MultiBinding Converter="{StaticResource BetweenTextSeparatorConverter}" ConverterParameter=" · " Mode="OneWay">
                                        <Binding Path="AssetDetails.Exchange" Mode="OneWay"/>
                                        <Binding Path="AssetDetails.BrokerName" Mode="OneWay"/>
                                    </MultiBinding>
                                </Run.Text>
                            </Run>
                            <Run Text="{Binding AssetDetails.BrokerName, Mode=OneWay}"/>
                            <Run>
                                <Run.Text>
                                    <MultiBinding Converter="{StaticResource BetweenTextSeparatorConverter}" ConverterParameter=" · " Mode="OneWay">
                                        <Binding Path="AssetDetails.BrokerName" Mode="OneWay"/>
                                        <Binding Path="AssetDetails.PortfolioName" Mode="OneWay"/>
                                    </MultiBinding>
                                </Run.Text>
                            </Run>
                            <Run Text="{Binding AssetDetails.PortfolioName, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding AssetDetails.IsActive, Mode=OneWay, Converter={StaticResource BoolToIconConverter}}"
                                   FontSize="24"
                                   HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding AssetDetails.IsActive, Mode=OneWay, Converter={StaticResource BoolToActiveStatusConverter}}"
                                   FontSize="11"
                                   Foreground="#666666"
                                   HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Tab Control for Details -->
            <TabControl Grid.Row="1">
                <!-- Summary Tab -->
                <TabItem Header="Summary">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Row 0: Quantity & Average Price -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Quantity:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AssetDetails.Quantity, Mode=OneWay, StringFormat=N8}" TextAlignment="Right" FontFamily="Consolas" Margin="10,5"/>

                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Average Price:" FontWeight="Bold" Margin="20,5,0,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding AssetDetails.AveragePrice, Mode=OneWay, StringFormat=N2}" TextAlignment="Right" FontFamily="Consolas" Margin="10,5"/>

                            <!-- Row 1: ISIN -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="ISIN:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding AssetDetails.ISIN, Mode=OneWay}" Margin="10,5"/>

                            <!-- Separator -->
                            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" 
                                    BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Margin="0,15,0,15"/>

                            <!-- Row 3: Total Bought & Total Sold -->
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Total Bought:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" 
                                       Text="{Binding AssetDetails.TotalBought, Mode=OneWay, StringFormat=N2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Margin="10,5"
                                       Foreground="Green"/>

                            <TextBlock Grid.Row="3" Grid.Column="2" Text="Total Sold:" FontWeight="Bold" Margin="20,5,0,5"/>
                            <TextBlock Grid.Row="3" Grid.Column="3" 
                                       Text="{Binding AssetDetails.TotalSold, Mode=OneWay, StringFormat=N2}" 
                                       Margin="10,5"
                                       Foreground="Red"/>

                            <!-- Row 4: Total Credits -->
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Total Credits:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" 
                                       Text="{Binding AssetDetails.TotalCredits, Mode=OneWay, StringFormat=N2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Margin="10,5"
                                       Foreground="Blue"/>

                            <!-- Separator -->
                            <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4" 
                                    BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Margin="0,15,0,15"/>

                            <!-- Row 6: Current Header -->
                            <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Text="Current" FontWeight="Bold" FontSize="13" Margin="0,5"/>
                            <Button Grid.Row="6" Grid.Column="2" Grid.ColumnSpan="2"
                                    Command="{Binding AssetDetails.RefreshTodayInfoCommand}"
                                    Content="Refresh"
                                    HorizontalAlignment="Right"
                                    Margin="0,5"/>

                            <!-- Row 7: Current Value & As Of -->
                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Current Value:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="7" Grid.Column="1" 
                                       Text="{Binding AssetDetails.TodayCurrentValue, Mode=OneWay, StringFormat=N2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Margin="10,5"/>
                            <TextBlock Grid.Row="7" Grid.Column="2" Text="As of:" FontWeight="Bold" Margin="20,5,0,5"/>
                            <TextBlock Grid.Row="7" Grid.Column="3" 
                                       Text="{Binding AssetDetails.TodayCurrentValueAsOf, Mode=OneWay}" 
                                       Margin="10,5"/>

                            <!-- Row 8: Total Current Value & Result % -->
                            <TextBlock Grid.Row="8" Grid.Column="0" Text="Total Current Value:" FontWeight="Bold" Margin="0,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Grid.Row="8" Grid.Column="1" 
                                       Text="{Binding AssetDetails.TotalCurrentValue, Mode=OneWay, StringFormat=N2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Margin="10,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Grid.Row="8" Grid.Column="2" Text="Result %:" FontWeight="Bold" Margin="20,5,0,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Grid.Row="8" Grid.Column="3" 
                                       Text="{Binding AssetDetails.ResultPercent, Mode=OneWay, StringFormat=P2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Foreground="{Binding AssetDetails.ResultPercent, Converter={StaticResource SignedValueToBrushConverter}}"
                                       Margin="10,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>

                            <!-- Row 9: Total Current + Credits & Result % -->
                            <TextBlock Grid.Row="9" Grid.Column="0" Text="Total Current + Credits:" FontWeight="Bold" Margin="0,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Grid.Row="9" Grid.Column="1" 
                                       Text="{Binding AssetDetails.TotalCurrentValueWithCredits, Mode=OneWay, StringFormat=N2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Margin="10,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Grid.Row="9" Grid.Column="2" Text="Result %:" FontWeight="Bold" Margin="20,5,0,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Grid.Row="9" Grid.Column="3" 
                                       Text="{Binding AssetDetails.ResultPercentWithCredits, Mode=OneWay, StringFormat=P2}" 
                                       TextAlignment="Right"
                                       FontFamily="Consolas"
                                       Foreground="{Binding AssetDetails.ResultPercentWithCredits, Converter={StaticResource SignedValueToBrushConverter}}"
                                       Margin="10,5"
                                       Visibility="{Binding AssetDetails.HasAveragePrice, Converter={StaticResource BoolToVisibilityConverter}}"/>

                            <!-- Row 10: Status/Error -->
                            <TextBlock Grid.Row="10" Grid.Column="0" Text="Status:" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Grid.Row="10" Grid.Column="1" Grid.ColumnSpan="3"
                                       Text="{Binding AssetDetails.TodayInfoMessage, Mode=OneWay}" 
                                       Margin="10,5"
                                       Foreground="DarkRed"/>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- Operations Tab -->
                <TabItem Header="Operations">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
                            <Button Command="{Binding AssetDetails.AddOperationCommand}" ToolTip="New operation">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" Margin="0,0,6,0"/>
                                    <TextBlock Text="New"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <DataGrid ItemsSource="{Binding AssetDetails.Operations}"
                                  SelectedItem="{Binding AssetDetails.SelectedOperation, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick"
                                              Command="{Binding AssetDetails.UpdateOperationCommand}"/>
                            </DataGrid.InputBindings>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Command="{Binding DataContext.AssetDetails.UpdateOperationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Update"
                                                        Padding="2"
                                                        Margin="0,0,4,0">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;"/>
                                                </Button>
                                                <Button Command="{Binding DataContext.AssetDetails.DeleteOperationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Delete"
                                                        Padding="2">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Date" 
                                                    Binding="{Binding Date, Converter={StaticResource DateFormatConverter}, ConverterParameter='d'}" 
                                                    Width="Auto"/>
                                <DataGridTextColumn Header="Type" 
                                                    Binding="{Binding Type}" 
                                                    Width="Auto">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" 
                                                    Value="{Binding Type, Converter={StaticResource OperationTypeToColorConverter}}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Quantity" 
                                                    Binding="{Binding Quantity, StringFormat=N8}" 
                                                    Width="Auto">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Unit Price" 
                                                    Binding="{Binding UnitPrice, StringFormat=N2}" 
                                                    Width="Auto">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Fees" 
                                                    Binding="{Binding Fees, StringFormat=N2}" 
                                                    Width="Auto">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Total" 
                                                    Binding="{Binding TotalPrice, StringFormat=N2}" 
                                                    Width="*"
                                                    IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextAlignment" Value="Right"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="LightYellow"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="primitives:Selector.IsSelectionActive" Value="False"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="LightYellow"/>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="LightYellow"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="primitives:Selector.IsSelectionActive" Value="False"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Background" Value="LightYellow"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>
                </DockPanel>
            </TabItem>

                <!-- Credits Tab -->
                <TabItem Header="Credits">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                            <Button Command="{Binding AssetDetails.AddCreditCommand}" ToolTip="New credit">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" Margin="0,0,6,0"/>
                                    <TextBlock Text="New"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding AssetDetails.Credits}"
                                  SelectedItem="{Binding AssetDetails.SelectedCredit, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick"
                                              Command="{Binding AssetDetails.UpdateCreditCommand}"/>
                            </DataGrid.InputBindings>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Command="{Binding DataContext.AssetDetails.UpdateCreditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Update"
                                                        Padding="2"
                                                        Margin="0,0,4,0">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;"/>
                                                </Button>
                                                <Button Command="{Binding DataContext.AssetDetails.DeleteCreditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Delete"
                                                        Padding="2">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Date" 
                                                    Binding="{Binding Date, Converter={StaticResource DateFormatConverter}, ConverterParameter='d'}" 
                                                    Width="Auto"/>
                                <DataGridTextColumn Header="Type" 
                                                    Binding="{Binding Type}" 
                                                    Width="Auto"/>
                                <DataGridTextColumn Header="Value" 
                                                    Binding="{Binding Value, StringFormat=N2}" 
                                                    Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextAlignment" Value="Right"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="LightYellow"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="primitives:Selector.IsSelectionActive" Value="False"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="LightYellow"/>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="LightYellow"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="primitives:Selector.IsSelectionActive" Value="False"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="LightYellow"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                        </DataGrid>
                        <GridSplitter Grid.Row="2"
                                      Height="6"
                                      HorizontalAlignment="Stretch"
                                      Background="#E0E0E0"
                                      ResizeDirection="Rows"
                                      ResizeBehavior="PreviousAndNext"/>
                        <datavis:Chart Grid.Row="3"
                                       Title="Credits by Month">
                            <datavis:Chart.LegendStyle>
                                <Style TargetType="Control">
                                    <Setter Property="Width" Value="0" />
                                    <Setter Property="Height" Value="0" />
                                </Style>
                            </datavis:Chart.LegendStyle>
                            <datavis:ColumnSeries Title=""
                                                  DependentValuePath="Value"
                                                  IndependentValuePath="Key"
                                                  ItemsSource="{Binding AssetDetails.CreditsByMonthChart}"/>
                        </datavis:Chart>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- Empty State -->
            <StackPanel Grid.Row="1" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Visibility="{Binding AssetDetails.AssetName, Mode=OneWay, Converter={StaticResource EmptyStringToVisibilityConverter}}">
                <TextBlock Text="Select an asset to view details" 
                           FontSize="16" 
                           Foreground="#999999"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>












