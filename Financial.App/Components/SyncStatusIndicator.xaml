<UserControl x:Class="Financial.Presentation.App.Components.SyncStatusIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="600">
    <Border Visibility="{Binding IsIndicatorVisible, Converter={StaticResource BoolToVisibilityConverter}}"
            Background="#FDECEA"
            BorderBrush="#F5C6CB"
            BorderThickness="0,0,0,1"
            Padding="16,8">
        <ItemsControl ItemsSource="{Binding IndicatorMessages}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="#C0392B" TextWrapping="Wrap"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
