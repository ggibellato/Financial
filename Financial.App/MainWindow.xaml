<Window x:Class="Financial.Presentation.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Financial.Presentation.App.Components"
        mc:Ignorable="d"
        Title="Financial tools" Height="800" Width="1200" Icon="{StaticResource AppIcon}"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding IsCollapsed, Converter={StaticResource BoolToSidebarWidthConverter}}"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <local:Sidebar Grid.Column="0"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <local:SyncStatusIndicator DataContext="{Binding SyncStatus}"/>
                <local:PaymentDueBanner DataContext="{Binding PaymentsDue}"/>
            </StackPanel>
            <Border Grid.Row="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="16,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding BreadcrumbText}" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               AutomationProperties.Name="Breadcrumb"/>
                    <local:ColourModeToggleButton Grid.Column="1" DataContext="{Binding ColourMode}" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            <ContentControl Grid.Row="2" Content="{Binding SelectedContent}"/>
        </Grid>
    </Grid>
</Window>
