> Part of the `testing-guide-Financial` skill (see `../SKILL.md`).

# Research Notes — Phase 2 web research log (2026-09-06)

Ten searches, curated against the versions detected in Phase 1 (.NET 10, xUnit 2.9.3 on
VSTest, ASP.NET Core 10 / `Microsoft.AspNetCore.Mvc.Testing` 10.0.6, WPF-UI 4.0.1, React 19.2,
Vitest 4.1.11 installed, RTL 16.3, Fluent UI v9.74, Playwright 1.61). Advice targeting xUnit v3
or Microsoft.Testing.Platform, Jest, or Cypress was discarded.

| Topic searched | Query / search terms | Key finding (or "nothing usable found") | Version scope | Informed |
|---|---|---|---|---|
| 1. Framework testing best practices | `ASP.NET Core 10 integration testing WebApplicationFactory best practices ConfigureTestServices` | `ConfigureTestServices` should replace only external boundaries; override `ConfigureWebHost`; for external HTTP inject an `HttpClient` with your own `HttpMessageHandler` — matches `ApiTestFactory`'s two overrides exactly | ASP.NET Core 8–10 (devleader.ca 2026-06, antondevtips, oneuptime 2025-12) | `../SKILL.md` §1 mock boundary; `mock-health-rules.md` table; `../artifacts/api-controllers-and-middleware.md` |
| 2a. How to test artifact types — categorisation | `xunit 2.9 Trait attribute dotnet test --filter syntax category tests` | `[Trait("Key","Value")]` filtered with `--filter "Key=Value"`; `~` contains; both key and value required; VSTest filter grammar (`&`, `\|`, `!=`) | xUnit 2.x on VSTest (learn.microsoft.com selective-unit-tests, vstest filter.md) | `feature-traceability.md` tagging + audit command (dry-run verified with `--list-tests`); `gotchas.md` |
| 2b. How to test artifact types — React | `Vitest 4 React Testing Library best practices vi.mock module hoisted fake timers 2026` | `vi.mock` is hoisted above imports, so referenced mocks must come from `vi.hoisted`; wrap timer advances in `act`, restore timers in `afterEach`; prefer `runAllTimersAsync`/`advanceTimersByTimeAsync` | Vitest 3–4 (vitest.dev/api/vi, qaskills 2026 guide) | `../artifacts/react-hooks.md` setup; `gotchas.md` (Vitest section) |
| 2c. How to test artifact types — WPF | `WPF MVVM ViewModel unit testing xunit without STA thread pitfalls Dispatcher` | xUnit runs MTA; `Application.Current` is null in tests; keep `Dispatcher` out of ViewModels or abstract it; `Xunit.StaFact` exists if a real STA is ever needed | xUnit 2.x / WPF (thomasclaudiushuber, Xunit.StaFact) | `../artifacts/wpf-viewmodels.md` layer notes; `../SKILL.md` §1 version-specific line |
| 2d. How to test artifact types — Fluent UI | `Fluent UI React v9 testing library jsdom pitfalls tabster FluentProvider vitest` | Fluent's own suite uses RTL + jsdom but moves focus-management tests to a real browser; components need a `FluentProvider` + theme or render without visuals; nothing Vitest-specific found (negative result) — the project's `setupTests.ts` `createRequire` shim is a local discovery, not documented upstream | Fluent UI React v9 (microsoft/fluentui wiki) | `../artifacts/react-components.md`; `gotchas.md`; `cross-cutting-concerns.md` §3 |
| 3a. In-process integration patterns — .NET | (covered by search 1 and 5) | Real services + fake `HttpMessageHandler` is the documented ecosystem pattern; WireMock.Net is the alternative when server-level simulation is needed — not adopted (no extra dependency needed at this scale) | ASP.NET Core 8–10 | `../artifacts/external-http-services.md`; `external-providers.md` |
| 3b. In-process integration patterns — time | `Microsoft.Extensions.TimeProvider.Testing FakeTimeProvider testing debounce timers .NET` | `FakeTimeProvider.Advance` fires `ITimer`s created from it; the application must advance explicitly; deterministic timer tests without sleeps | .NET 8+ (`Microsoft.Extensions.Time.Testing`; learn.microsoft.com, andrewlock.net) | `cross-cutting-concerns.md` §1; `../artifacts/json-persistence-and-repositories.md`; `gotchas.md` (timer-not-armed pitfall) |
| 3c. AC-tagging in Vitest | `vitest test name filter -t regex tagging acceptance criteria tests convention` | Vitest 4 has `--tagsFilter` with `&&`/`\|\|`/`!` and `@module-tag`; **but** tags must be declared in `test.tags` (`strictTags` default true) — confirmed locally with an actual "does't define any tags" error; `-t` regex on the full name is the alternative | Vitest 4.1 (vitest.dev/guide/test-tags, /guide/filtering) | `feature-traceability.md` (Web uses `[AC …]` title + `-t`); `gotchas.md` |
| 4. Multi-service / production-like E2E orchestration | `Playwright end-to-end testing against docker-compose stack ASP.NET Core API React SPA CI` | Full-stack Playwright in CI is standard (SendRec 2026: Go+Postgres+S3 in CI); docker-compose with health checks before tests; GitHub Actions can run Playwright in a container; the project's smoke job already implements the equivalent with a published process + `curl` health poll | Playwright 1.4x–1.6x | `e2e-environment.md`; `../artifacts/future-types.md` (Playwright suite) |
| 5. Mock vs real for external providers | `testing HttpClient in .NET fake HttpMessageHandler vs WireMock.Net external API best practice` | Fake handler: no dependency, per-test control, must be injected (cannot fake a `new HttpClient()`); WireMock.Net: richer matching, same-process server, useful for delays. Decision: keep the fake handler, move it to `Financial.TestUtilities` on the next provider | .NET 6+ (devleader.ca 2026-07, damirscorner) | `external-providers.md`; `../artifacts/external-http-services.md` |
| 6a. Common pitfalls — architecture tests | `architecture fitness function tests .NET NetArchTest vs reflection GetReferencedAssemblies` | `GetReferencedAssemblies()` is enough for assembly-level rules; NetArchTest/ArchUnitNET add type-level rules; keep reflection until a type-level rule is needed | .NET generic (ben-morris, milanjovanovic, code-maze) | `../artifacts/architecture-rule-tests.md` "When to skip" |
| 6b. Common pitfalls — stack | (from searches 2b, 2d, 3b) | Timer-not-armed with fake clocks; hoisting; jsdom focus; strict tags; `mockReset` vs `mockClear` | as above | `gotchas.md` |

Nothing found contradicted the existing suite's patterns; the two actionable changes were
(a) using `--tagsFilter` is not viable without config changes, hence the title-prefix
convention for Web, and (b) confirming that the fake-`HttpMessageHandler` approach remains the
recommended default over adding WireMock.Net.
